#! /usr/bin/env node
'use strict';

/*
const utils = require('./lib/utils');
const DDFRoot = require('./lib/data/root');
const ddfRoot = new DDFRoot(utils.ddfRootFolder);

ddfRoot.check(() => {
  ddfRoot.getDdfDirectoriesDescriptors().forEach(dd => {
    console.log(`${dd.dir} ${dd.isEmpty} ${dd.isDDF}`);
  });
});
*/
